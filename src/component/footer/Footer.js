import React from 'react';
import styles from './footer.module.css'

const Footer = () => {
  return (
    <div className={`bg-success text-center text-white ${styles.footer}`}>
      This is footer
    </div>
  );
}

export default Footer;
