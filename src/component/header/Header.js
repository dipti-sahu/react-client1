import React from 'react';
import styles from './header.module.css'

const Header = () => {
  return (
    <div className={`bg-primary text-center text-white ${styles.header}`}>
      Header
    </div>
  );
}

export default Header;
